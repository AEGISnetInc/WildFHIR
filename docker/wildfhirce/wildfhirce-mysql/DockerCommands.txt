# -------------------------------------------------------------------------------
# Build wildfhirce-mysql multi-arch image
# - Build SBOM attestation and a provenance attestation with max mode provenance
# - Named tag v0.7.0
# -------------------------------------------------------------------------------
docker buildx build --provenance=true --sbom=true --platform linux/arm64,linux/amd64 -t aegisnetinc/wildfhirce-mysql:v0.7.0 .
